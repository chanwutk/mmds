.venv/
.idea/
.idea_backup/
__pycache__/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# YOLOE / Ultralytics artifacts (downloaded when running Detect examples)
mobileclip_blt.ts
yoloe-11s-seg.pt

# macOS metadata
.DS_Store

# SoccerNet dataset (downloaded separately — not committed)
data/soccernet/

# Generated video clips (ffmpeg output — large, reproducible from source)
data/match_clips/
data/soccer_clips/

# Whisper transcripts (generated by transcribe_*.py scripts)
data/transcripts/

# Gemini Files API URI cache (ephemeral, per-machine)
data/gemini_uri_cache.json

# Per-machine data files with absolute local paths
data/matches.jsonl
data/soccer_goal_clips.jsonl
data/soccer_clips.jsonl

# Unrelated traffic example data
data/traffic*.jsonl

# Scratch files from interactive experiments
examples/frames/
examples/expr_results
